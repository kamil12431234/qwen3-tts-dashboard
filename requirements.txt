# Qwen3-TTS Dashboard — core dependencies

fastapi>=0.115
uvicorn[standard]>=0.29
pydantic>=2.7
starlette>=0.41

torch>=2.4.0
torchaudio>=2.4.0
transformers>=4.47.0
tokenizers>=0.20.0
safetensors>=0.4.0
accelerate>=1.0.0
einops>=0.8.0

numpy>=1.26.0
soundfile>=0.12.0
python-multipart>=0.0.9

# TTS backend (required)
faster-qwen3-tts>=0.2.5

# Note: install torch with CUDA wheels if using GPU, e.g.:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu124
